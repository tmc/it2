// Package tool provides direct access to built-in session plugins.
//
// The tool command exposes all discovered plugins as subcommands,
// allowing them to be invoked directly via the CLI.
package tool
